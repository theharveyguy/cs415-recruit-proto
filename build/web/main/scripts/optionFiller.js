/* 
 * A script to fill in the gaps in athlete, recruiter, and school forms.
 */
var filler = (function(){
    return{
        optionFiller: function(query){
            // accepts query as an argument so that it should be able to be used many times
            // needs to send query through using ajax
        }
    };
}());

